# block_info/basis
class BlockInfo:
    (BlockType)
    (InputInfo, DropdownInfo)
    (InputType, DropdownType, InputMode)


class BlockInfoSet:
    (BlockInfo)
    (SRBlock)

# block_info/__init__
class BlockInfoApi:
    (BlockInfoSet)

