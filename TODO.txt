TODOs Sorted by Priorites

Special:
    TODOs in Code + read through code, optimize here and there
    Add documentation for basic stuff:
        - Creating (SRProject > FRProject.to_file)
        - Editing (FRProject.from_file > SRProject > FRRoject.to_file)
        - Inspecting (FRPProject.from_file > SRProject)

High: /

Medium:
    - XL: test and document opcode_info
    - XL: spit into internal and public functions/methods etc (easier in VSCode)
        => add enforce_argument_types decorator to public ones
        => remove "Returns" section of docstrings to avoid doubling (easy to do simultaneously)
        => rethink error strategy (e.g. ValueError, TypeError) => should i wrap external errors instead of mine?
        => error formatting
    - M/L: make a script which show info for the requested block opcode
    - M: implement difficult polygon block
    - [M.OPT]: add FRProject.from_file alternative, fetch from project link 
    - S/M: implement variable_type and variable_id (example: "small_example")
    - S: explore extensionData (example of "2D MAINCRAFT")
    - S: add "previous costume" and "next costume" to costume menu
        => explore for similar updates
    - S: add new rotation style(s)
    - S: fix grepr multiline string issue
        => also fix parent and subclass field order issue
    - S: investigate custom type object variables(example: vector extension)
    - S: add references at the end of call doc files

Low:
    - XL: test and document utility
    - L: simplify new opcode input/dropdown representation brackets
    - M: replace remaining io actions with wrappers (eg. file_exists instead of os.path.exists)
    - M: possibly reform special case system with custom classes possibly even with metaclass
    - S: add copy method to OpcodeInfoAPI and possibly others
    - S: check if dropdown validation where only default kind: allows non strings? (should not)
    - S: possibly remove mode attribute of IRInputValue
    - S: check uses of ALL_FR_BLOCKS_CLEAN vs. ALL_FR_BLOCKS
    - S: explore "customFonts" (example of "2D MAINCRAFT")
    - S: create CONTRIBUTING.md
    - S: stop using aenum and find better solution for ext_info_gen instead of extend_enum
    - S: enable slots by default
    - S: add enforce_argument_types to __init__ in grepr_dataclass

Formatting:
    - L: add all errors to docstrings
    - M: add specific reasons for ThanksError
    - M: remove "Returns" section of docstrings to avoid doubling
    - S: rename block_interface to sth like transformation_interface
    - S: relax __all__, include more. Users should not do "from pmp_manip import *" anyway.
