TODOs:
    - todos + go over code
    - test and document utility
    - test and document opcode_info
    - add specific reasons for ThanksError
    - add all errors to docstrings
    - possibly remove mode attribute of IRInputValue
    - check uses of ALL_FR_BLOCKS_CLEAN vs. ALL_FR_BLOCKS
    - change typing of dropdown values
    - rename block_interface to sth like transformation_interface
    - check for need of copy and deepcopy in transformation methods
    - remove "Returns" section of docstrings to avoid doubling
    - add "previous costume" and "next costume" to costume menu
        => explore for similar updates
    - check if dropdown validation where only default kind: allows non strings? (should not)
    - make sure: js execution in ext_info_gen is safe
    - simplify new opcode input/dropdown representation brackets
